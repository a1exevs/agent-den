{
  "name": "agent-den",
  "version": "0.0.0",
  "private": true,
  "description": "A living pixel-art den where your AI agents (and their subagents) are cats",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || >=26.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:collector": "npm run dev -w @agent-den/collector",
    "dev:mock": "npm run start -w @agent-den/mock",
    "dev:web": "npm run start -w @agent-den/web",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "node scripts/sync-agent-rules.mjs --check && node scripts/check-structure.mjs && prettier --check . && knip && npm run lint --workspaces --if-present",
    "lint:structure": "node scripts/check-structure.mjs",
    "lint:unused": "knip",
    "rules:sync": "node scripts/sync-agent-rules.mjs",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@alexevs/prettier-config": "1.0.1",
    "knip": "6.38.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3"
  }
}
