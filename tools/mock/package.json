{
  "name": "@agent-den/mock",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only generator that replays fake agent scenarios into the collector",
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "lint": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@agent-den/contracts": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "tsx": "4.23.15"
  }
}
